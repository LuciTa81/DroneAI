# Home RTX 5090 Backend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add and verify an additive `home5090_docker` runtime/storage backend without changing historical Colab provenance or starting long training.

**Architecture:** Validated JSON profiles provide all new runtime and storage paths. Stage 0 consumes a profile, runs a synthetic CUDA matrix smoke, and writes a hash-bearing run manifest; model overlays stay isolated from the NGC base environment.

**Tech Stack:** Python 3.12, pytest, PyTorch, JSON profiles, Docker, SSH, GitHub Actions

## Global Constraints

- Preserve Draft PR #4, the current feature branch, historical Colab notebooks, and prior Drive results.
- Make `home5090_docker` the documented default and keep `colab_drive` as fallback.
- Never delete or move existing files under `/home/lucita/crowd-counting-lab` or personal NTFS storage.
- Store large data only under `/workspace/data`, bound from `/mnt/crowd-data/CrowdCounting`.
- Do not transfer datasets, install unreviewed weights, or start long training.
- Keep research-only and commercial/production approval scopes distinct.

---

### Task 1: Runtime profiles and containment guard

**Files:**
- Create: `configs/runtime/colab_drive.json`
- Create: `configs/runtime/home5090_docker.json`
- Create: `src/droneai/runtime_profile.py`
- Create: `tests/test_runtime_profile.py`

**Interfaces:**
- Produces: `RuntimeProfile`, `load_runtime_profile(path)`, and `assert_storage_layout(profile)`.

- [ ] Write tests that require absolute paths, exact home5090 paths, and rejection of a child path outside `storage.root`.
- [ ] Run `py -m pytest tests/test_runtime_profile.py -q` and confirm import failure.
- [ ] Implement the smallest immutable profile loader and containment validation.
- [ ] Re-run the focused tests and confirm they pass.

### Task 2: Profile-driven Stage 0 manifest

**Files:**
- Modify: `src/droneai/stage0.py`
- Modify: `src/droneai/runtime_probe.py`
- Modify: `scripts/run_stage0.py`
- Modify: `tests/test_stage0.py`

**Interfaces:**
- Consumes: `RuntimeProfile` from Task 1.
- Produces: `run_stage0(..., profile=RuntimeProfile | None)` and `run_manifest.json` with artifact SHA-256 entries.

- [ ] Add tests for profile metadata, synthetic dataset/split nulls, research-only scope, and 64-character recomputed artifact hashes.
- [ ] Run the focused tests and confirm the new assertions fail.
- [ ] Generalize Stage 0 storage names while preserving `drive_root` compatibility, change the CUDA blocker to deterministic matrix multiplication, and write the manifest.
- [ ] Re-run the focused tests and confirm they pass.

### Task 3: Explicit Stage 3 paths and CI parity

**Files:**
- Modify: `scripts/run_stage3a_faithful.py`
- Modify: `scripts/run_stage3b_clean_smoke.py`
- Modify: `scripts/evaluate_stage3a_pilot.py`
- Modify: `tests/test_stage3a_faithful.py`
- Modify: `tests/test_stage3a_pilot.py`
- Modify: `pyproject.toml`

**Interfaces:**
- Produces: Stage 3 parsers that require `--upstream-dir`; GitHub and local `pytest` resolve `scripts` identically.

- [ ] Add parser tests that fail while `/content/DM-Count` remains an implicit default.
- [ ] Run the focused tests and confirm failure for the expected default.
- [ ] Require the upstream path, remove Colab-only runtime wording, and add the repository root to pytest's configured import path.
- [ ] Run `pytest -q` and confirm the GitHub Actions entrypoint passes.

### Task 4: Environment isolation and operator runbook

**Files:**
- Create: `requirements/models/dm_count-home5090.lock.txt`
- Modify: `.gitignore`
- Create: `docs/HOME5090_RUNBOOK.md`
- Modify: `README.md`
- Modify: `docs/STAGE_0.md`

**Interfaces:**
- Produces: exact laptop commands for SSH, safe sync/pull, harness smoke, model venv creation, future train/resume, and small-result pull.

- [ ] Pin only the DM-Count overlay dependencies; inherit PyTorch from `nvcr.io/nvidia/pytorch:25.10-py3` through `--system-site-packages`.
- [ ] Document that image digest and installed package versions are captured before a model run.
- [ ] Document Stage 3C, dataset rights, transfer, and hash gates before any training.
- [ ] Confirm no new documentation presents a research smoke as commercial or production approval.

### Task 5: Local, GitHub, and home5090 verification

**Files:**
- Update: existing Draft PR #4 through its current branch.
- Create on server: Git metadata and generated smoke results only; preserve existing files.

**Interfaces:**
- Produces: a pushed commit and server-side `run_manifest.json`, `score.json`, `score.md`, `environment.json`, and config snapshot.

- [ ] Run the full local suite and inspect the complete diff and status.
- [ ] Commit only intended files and push `agent/stage3-dm-count-reproduction`.
- [ ] Initialize/fetch the repo safely at `/home/lucita/crowd-counting-lab`, preserving `data/` and `test/startcodex.txt`.
- [ ] Run the full harness and Stage 0 CUDA smoke inside `crowd-jupyter`.
- [ ] Recompute artifact hashes, compare Git HEADs, and report PR/CI/server evidence without starting model training.
